export const API_BASE_URL = 'http://localhost:8080/api'

/*
  No futuro, este arquivo concentrará as requisições ao backend.

  Exemplo futuro:

  export async function buscarTurmas() {
    const resposta = await fetch(`${API_BASE_URL}/turmas`)
    return resposta.json()
  }
*/